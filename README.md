### Как запускать?

1. Убедитесь что у вас установлен `node` и `docker`
2. Выполните команду `yarn bootstrap` - это обязательный шаг, без него ничего работать не будет :)
3. Выполните команду `yarn dev`
4. Выполните команду `yarn dev --scope=client` чтобы запустить только клиент
5. Выполните команду `yarn dev --scope=server` чтобы запустить только server

### Как добавить зависимости?

В этом проекте используется `monorepo` на основе [`lerna`](https://github.com/lerna/lerna)

Чтобы добавить зависимость для клиента
`yarn lerna add {your_dep} --scope client`

Для сервера
`yarn lerna add {your_dep} --scope server`

И для клиента и для сервера
`yarn lerna add {your_dep}`

Если вы хотите добавить dev зависимость, проделайте то же самое, но с флагом `dev`
`yarn lerna add {your_dep} --dev --scope server`

### Тесты

Для клиента используется [`react-testing-library`](https://testing-library.com/docs/react-testing-library/intro/)

`yarn test`

### Линтинг

`yarn lint`

### Форматирование prettier

`yarn format`

### Production build

`yarn build`

И чтобы посмотреть что получилось

`yarn preview --scope client`
`yarn preview --scope server`

## Хуки

В проекте используется [lefthook](https://github.com/evilmartians/lefthook)
Если очень-очень нужно пропустить проверки, используйте `--no-verify` (но не злоупотребляйте :)

## Ой, ничего не работает :(

Откройте issue, я приду :)

## Автодеплой статики на vercel

Зарегистрируйте аккаунт на [vercel](https://vercel.com/)
Следуйте [инструкции](https://vitejs.dev/guide/static-deploy.html#vercel-for-git)
В качестве `root directory` укажите `packages/client`

Все ваши PR будут автоматически деплоиться на vercel. URL вам предоставит деплоящий бот

## Dev окружение в докере

Перед первым запуском выполните `node init.js`

`docker compose -f docker-compose.dev.yml up --build -d` - Работа с hot reload

После успешного запуска приложение будет доступно по адресу `http://localhost:PORT`, где `PORT` — это значение `SERVER_PORT` из вашего `.env` файла.

Приложение запускается в одном контейнере app, который объединяет клиентскую и серверную часть

## Production окружение в докере

Перед первым запуском выполните `node init.js`

`docker compose up --build -d` - Эта команда соберет образ вашего приложения и запустит все необходимые сервисы в фоновом режиме.

После успешного запуска приложение будет доступно по адресу `http://localhost:PORT`, где `PORT` — это значение `SERVER_PORT` из вашего `.env` файла.

Приложение запускается в одном контейнере app, который объединяет клиентскую и серверную часть

# 🦕 Dino Clicker – Механики игры

В игре есть 10 видов улучшений, которые автоматически приносят вам очки. С каждым последующим уровнем улучшения становятся **мощнее**, но при этом **дороже**.

---

## 📈 Основные правила:

- ➕ **Покупка улучшения** активирует автоматическое производство очков.
- 💰 **Чем больше экземпляров** одного улучшения вы купите, тем дороже будет следующий.
- ⚡ **Мощность увеличивается** с каждым уровнем улучшения.

---

## 🖱️ Сколько даёт один клик?

Формула:
Кол-во очков за клик = 1% от значения текущего счёта

Примеры:

- Счёт = `1K` → клик = `10`
- Счёт = `1M` → клик = `10,000`
- Счёт = `0` → клик = `1`

📌 Чем выше ваш счёт, тем мощнее становится каждый клик.

---

## 🔢 Как работают сокращения чисел

Игра использует сокращение чесел, чтобы отображать большие числа кратко и удобно:

### 🔍 Примеры:

| Число             | Отображение |
| ----------------- | ----------- |
| 999               | 999         |
| 1,000             | 1K          |
| 12,345            | 12.35K      |
| 1,000,000         | 1M          |
| 1,500,000         | 1.5M        |
| 1,000,000,000     | 1B          |
| 2,450,000,000,000 | 2.45T       |

### 📚 Список поддерживаемых суффиксов:

| Суффикс |          Значение | Название    |
| ------: | ----------------: | ----------- |
|     `K` |             1_000 | Тысяча      |
|     `M` |         1_000_000 | Миллион     |
|     `B` |     1_000_000_000 | Миллиард    |
|     `T` | 1_000_000_000_000 | Триллион    |
|    `Qa` |              1e15 | Квадриллион |
|    `Qi` |              1e18 | Квинтиллион |
|    `Sx` |              1e21 | Секстиллион |
|    `Sp` |              1e24 | Септиллион  |
|    `Oc` |              1e27 | Октиллион   |
|    `No` |              1e30 | Нониллион   |
|    `Dc` |              1e33 | Дециллион   |

## 💎 Улучшения и их характеристики

| Уровень | Название          |  Цена первого | Очков/сек (1 шт) | Рост цены |
| ------- | ----------------- | ------------: | ---------------: | --------: |
| 1       | Камень            |             4 |                1 |        5% |
| 2       | Кактус            |            64 |                8 |        7% |
| 3       | Птеродактиль      |           508 |               48 |        6% |
| 4       | Тираннозавр       |         4,032 |              288 |        5% |
| 5       | Кибердинозавр     |        31,948 |            1,728 |        4% |
| 6       | Робозавр XT       |       255,590 |           10,368 |        4% |
| 7       | Дино-Бот 3000     |     2,035,392 |           62,208 |      3.5% |
| 8       | ДиноТрон 9000     |    16,400,499 |          373,248 |        3% |
| 9       | Армагеддон        |   128,345,702 |        2,239,488 |        2% |
| 10      | ДиноСингулярность | 1,026,765,619 |       13,436,928 |        2% |

---

## 🔢 Как рассчитывается?

### 💰 Стоимость N-го экземпляра:

Цена = Базовая цена × (1 + процент роста)^N

- Где `N` — сколько штук уже куплено.
- Пример: для 3-го Птеродактиля (baseCost 480, рост 6%)  
  `480 × 1.06^3 ≈ 571`

---

### ⚡ Производимая мощность:

Очков/сек = Базовая мощность × Кол-во купленных

- Каждое улучшение даёт фиксированное количество очков/сек, умноженное на число экземпляров.

---

## 💡 Советы для игроков

- 🏁 Начинайте с **дешёвых** улучшений – они быстрее окупаются.
- 🔢 Используйте покупку пачками по х10 х100 для того, чтобы скорее добиться желаемого результата.
- 📈 **Чем дороже апгрейд, тем больше он даёт очков** – но окупается дольше.
- 💾 Прогресс сохраняется автоматически в `localStorage`.

## Ссылка на видео с демонстрацией

(https://disk.yandex.com/i/wsDtyJmkG2rKRg)
